import { modeloJefecarre } from "../models/jefecarre.model.js";

const test = () => {
    console.log("Funciona la conexion a jefecarre")
}

export default test

modeloAlumnos.create({
    jefecarre_name: "Gerardo",
    jefecarre_last_name: "Ordeña"
})